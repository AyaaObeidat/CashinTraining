import { EvenMinutesDirective } from './even-minutes.directive';

describe('EvenMinutesDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenMinutesDirective();
    expect(directive).toBeTruthy();
  });
});
